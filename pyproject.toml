[tool.poetry]
name = "curso_de_python"
version = "0.1.0"
description = "Curso de Python Geek"
authors = ["Geek University"]

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
